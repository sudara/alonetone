var soundIsReady = true;

soundManager.setup({
  debugMode: false,
  flashVersion: 9,
  useHTML5Audio: true, /* for acl, baby */
  forceUseGlobalHTML5Audio: true,
  consoleOnly: true,
  url: '<%= asset_path('soundmanager2_flash9.swf')%>'
});
