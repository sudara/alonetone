
<div class="user_playlists" data-controller="playlists-sort">
  <h1><%= @user.name %>'s Playlists</h1>
  <ul class="playlists sortable"  data-target="playlists-sort.sortable">
    <%= render partial: 'shared/playlist_white', collection: @playlists, as: :playlist, cached: true %>
  </ul>
</div>
