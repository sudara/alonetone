<div id="user_listens" class="box">
	<h2 class="box">Recently listened to</h2>
	<%= render :partial => 'users/listen', :collection => @listens %>
</div>
<div class="footer_box">
    <%= link_to 'view listening history', listens_path(@user.login) if @listens.size > 4%>
</div>