<div class="sidebar">
    <div class="box">
        <h2 class="box static">Recent Grunts</h2>
        <div class="static_content">
            <ul>
            <% for update in @recent_updates %>
                <li><%= link_to update.title, blog_path(update.permalink) %></li>
            <% end %>
            </ul>
        </div>
    </div>
</div>
